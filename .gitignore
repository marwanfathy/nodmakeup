# dependencies
node_modules/

# builds
dist/
build/
.next/
out/

# environment — never commit real credentials
.env
.env.local
.env.*.local
.env.*
!.env.example

# runtime data
logs/
run/
*.log
media-server/public/uploads/
media-server/public/thumbnails/
media-server/public/temp/
media-server/temp/

# whatsapp web session state
.wwebjs_auth/
.wwebjs_cache/

# editor / os
.vscode/
.DS_Store

# hygiene — never commit envs or runtime dirs outside the root .env contract
deploy/env/*.env
backend/.runtime/